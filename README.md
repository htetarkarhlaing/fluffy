# Fluffy HUGS

An interactive picture-book landing page: three full-screen sections you scroll through, with a
character that travels between them. Built with **Next.js (App Router)**, **GSAP**, and
**Tailwind CSS v4**.

## Getting started

```bash
npm install
npm run dev      # http://localhost:3000
npm run lint
npm run build
```
